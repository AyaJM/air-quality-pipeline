apiVersion: batch/v1
kind: Job
metadata:
  name: airquality-job
spec:
  template:
    spec:
      containers:
      - name: airquality
        image: air-quality-pipeline
        imagePullPolicy: Never
      restartPolicy: Never
